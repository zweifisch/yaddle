// Generated by CoffeeScript 1.9.2
(function() {
  var parse, parseYAML, parseYaddle;

  parseYAML = function(input) {
    var charpos, cur, i, indent, j, lineno, prev, ref, results;
    indent = "";
    prev = null;
    lineno = 1;
    charpos = 1;
    results = [];
    for (i = j = 0, ref = input.length; 0 <= ref ? j < ref : j > ref; i = 0 <= ref ? ++j : --j) {
      cur = input.charAt(i);
      switch (cur) {
        case " ":
          switch (prev) {
            case null:
              throw Error("unexpected indentation at " + lineno + ":" + charpos);
              break;
            default:
              results.push(void 0);
          }
          break;
        default:
          results.push(void 0);
      }
    }
    return results;
  };

  parseYaddle = function(input) {};

  module.exports = {
    parse: parse = function(input) {
      return parseYaddle(parseYAML(input));
    },
    loads: function(file) {
      return new Promise(function(resolve, reject) {
        return fs.readFile(file, function(err, content) {
          if (err) {
            return reject(err);
          } else {
            return resolve(parse(content));
          }
        });
      });
    }
  };

}).call(this);
